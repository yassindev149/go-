on : push

name : Integration

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - name: Set up Go
        
        with:
          go-version: 1.18
      - name: Build
        run: |
          go build 